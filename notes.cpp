/*
1. insert the type to string method into actions and replace it under the add settlement class. and delete include cpp from the header.

2. check if returnung nullptr in bakanced policy is optional becasue it's only getts mark yellow and we dont have to deal with the case of facilities options being empty 

3. verify that type to string compiles in the action class under:  to string method (AddSettlement)


4. finish ChangePlanPolicy

5. check copy constractor of simulation with plan

6. selection policy row 1, decide if pragma once is needed because its in a comment

7. finish rule of 5 in simulation

*/